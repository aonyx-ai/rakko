typed_fields::name! {
    /// The extension of a file that a run of prettier examines
    ///
    /// The extension stands without its dot, the way a project writes it in a
    /// pattern: `md`, and not `.md`.
    FileExtension
}
