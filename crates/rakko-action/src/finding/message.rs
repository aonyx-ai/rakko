typed_fields::name! {
    /// The message that describes a problem
    ///
    /// A finding carries a message that tells a reader what the problem is. The
    /// message has no formatting; the machinery decides how to render it.
    FindingMessage
}
