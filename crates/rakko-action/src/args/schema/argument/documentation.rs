typed_fields::name! {
    /// The documentation of one argument
    ///
    /// The documentation is one sentence that tells a user what the argument
    /// does. A projection shows the sentence where a user looks for help, so
    /// the sentence stands on its own and names no flag and no other syntax.
    ///
    /// Every argument carries documentation. An argument that a user cannot
    /// read about is an argument that a user does not use.
    Documentation
}
