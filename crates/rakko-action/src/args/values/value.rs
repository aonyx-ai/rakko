typed_fields::name! {
    /// The value that a run reads for one argument
    ///
    /// The value holds the text that the machinery parsed for an argument.
    /// An argument set converts the text into the type of its field, so the
    /// text is the boundary between the machinery and the action.
    ArgumentValue
}
