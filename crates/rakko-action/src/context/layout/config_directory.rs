typed_fields::path! {
    /// The directory that holds the configuration of the tools of a project
    ///
    /// The default configuration directory is the `.config` directory in the
    /// project root. Configuration files declare how the tools of a project
    /// behave.
    ConfigDirectory
}
