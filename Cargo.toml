[workspace]
members = ["crates/*"]

# Opt-in to the new feature resolver introduced in Rust 1.85 and Edition 2024.
# https://doc.rust-lang.org/cargo/reference/resolver.html#resolver-versions
resolver = "3"

[workspace.package]
edition = "2024"

license = "Apache-2.0 OR MIT"
repository = "https://github.com/aonyx-ai/rakko"

# bon 3.10 requires Rust 1.88 or higher
rust-version = "1.88.0"

[workspace.dependencies]
bon = "3"
clap = "4.3"
clawless = "0.6.1"
getset = ">=0.1,<1"
kawauso-process = "0.2.1"
kawauso-project = "0.2.0"
rakko-action = { path = "crates/rakko-action", version = "0.1.0" }
rakko-build-internal-docs = { path = "crates/rakko-build-internal-docs", version = "0.1.0" }
rakko-cargo = { path = "crates/rakko-cargo", version = "0.1.0" }
rakko-check-dependencies = { path = "crates/rakko-check-dependencies", version = "0.1.0" }
rakko-check-latest-deps = { path = "crates/rakko-check-latest-deps", version = "0.1.0" }
rakko-check-minimal-deps = { path = "crates/rakko-check-minimal-deps", version = "0.1.0" }
rakko-check-msrv = { path = "crates/rakko-check-msrv", version = "0.1.0" }
rakko-check-unused-deps = { path = "crates/rakko-check-unused-deps", version = "0.1.0" }
rakko-format-json = { path = "crates/rakko-format-json", version = "0.1.0" }
rakko-format-markdown = { path = "crates/rakko-format-markdown", version = "0.1.0" }
rakko-format-rust = { path = "crates/rakko-format-rust", version = "0.1.0" }
rakko-format-yaml = { path = "crates/rakko-format-yaml", version = "0.1.0" }
rakko-lint-github-actions = { path = "crates/rakko-lint-github-actions", version = "0.1.0" }
rakko-lint-markdown = { path = "crates/rakko-lint-markdown", version = "0.1.0" }
rakko-lint-rust = { path = "crates/rakko-lint-rust", version = "0.1.0" }
rakko-lint-toml = { path = "crates/rakko-lint-toml", version = "0.1.0" }
rakko-lint-yaml = { path = "crates/rakko-lint-yaml", version = "0.1.0" }
rakko-nextest = { path = "crates/rakko-nextest", version = "0.1.0" }
rakko-prettier = { path = "crates/rakko-prettier", version = "0.1.0" }
rakko-taplo = { path = "crates/rakko-taplo", version = "0.1.0" }
rakko-test-rust = { path = "crates/rakko-test-rust", version = "0.1.0" }
rakko-tool = { path = "crates/rakko-tool", version = "0.1.0" }
rakko-worktree = { path = "crates/rakko-worktree", version = "0.1.0" }
serde = { version = "1.0.225", features = ["derive"] }
serde_json = "1.0.0"
tempfile = "3"
thiserror = "2"
tokio = { version = "1.27.0", features = ["fs", "macros", "rt", "time"] }
typed-fields = ">=0.6,<1"

[workspace.lints.clippy]
# Require documentation on private items as well, since that helps with long-term maintainability.
missing_docs_in_private_items = "warn"

# Require errors, panics, and unsafe code to be documented.
missing_errors_doc = "warn"
missing_panics_doc = "warn"
missing_safety_doc = "warn"
unnecessary_safety_doc = "warn"

# `unwrap` and `expect` can cause panics, which is why we ban them from production code. Tests are
# exempt, which `.clippy.toml` configures.
expect_used = "warn"
unwrap_used = "warn"

# Forbid printing anything to stdout or stderr.
print_stderr = "warn"
print_stdout = "warn"

# Prefer `mod.rs` over `name.rs` so that every module is a self-contained unit.
self_named_module_files = "warn"

[workspace.lints.rust]
# Detect explicit lifetime requirements that the compiler can infer.
explicit_outlives_requirements = "deny"

# Types must implement or derive `Debug` so that they can be easily debugged.
missing_debug_implementations = "deny"

# Require documentation for all public items
missing_docs = "deny"

# Public but unreachable items are likely a mistake.
unreachable_pub = "deny"

# Unsafe code can lead to undefined behavior and security vulnerabilities.
unsafe_code = "deny"
