typed_fields::name! {
    /// A minimum supported Rust version, as a manifest writes it
    ///
    /// The value names a release of the compiler, such as `1.88.0`, and it
    /// carries the text as it was written. A manifest and a pin of mise
    /// compare as text, so `1.88` and `1.88.0` are two values here.
    Msrv
}
