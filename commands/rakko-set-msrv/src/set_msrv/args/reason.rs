typed_fields::name! {
    /// Why a project needs its minimum supported Rust version
    ///
    /// The reason becomes the comment above `rust-version` in the root
    /// manifest, one comment line for each line of the reason.
    Reason
}
